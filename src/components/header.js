import React from 'react';

const Header = ({ siteTitle }) => (
  <div style={{ marginBottom: '1.45rem', padding: '1.45em 1.1em' }}>
    <h1 style={{ margin: 0, textAlign: 'center' }}>{siteTitle}</h1>
  </div>
);

export default Header;
